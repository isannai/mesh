{
  "spec_version": "1",
  "name": "probe",
  "version": "0.1.0",
  "author": "isann",
  "description": "faucet prober - asks public nodes a question and records the answer",
  "bin": "bin/probe",
  "args": [
    "-config",
    "conf/probe.json"
  ],
  "config": {
    "path": "config.json",
    "env": [
      {
        "key": "PROBE_SIGNER_PASSPHRASE",
        "required": false,
        "secret": true,
        "desc": "passphrase for the ticket-signing key; the key itself is found by the appointment's bound address in artifacts/keystores/"
      },
      {
        "key": "PROBE_KEYSTORES_DIR",
        "required": false,
        "secret": false,
        "desc": "override the keystore search directory (default: the install root's artifacts/keystores)"
      },
      {
        "key": "PROBE_GENERATORS",
        "required": false,
        "secret": false,
        "desc": "comma-list of nodes that write the questions, tried in turn: 'this' or a node id / favorite alias, optionally '<node>/<service>'. Use 'none' for arithmetic only (an empty value cannot mean that - it is indistinguishable from unset)"
      },
      {
        "key": "PROBE_GENERATOR_SERVICE",
        "required": false,
        "secret": false,
        "desc": "service name for generator entries that do not name one (default llm-api)"
      },
      {
        "key": "PROBE_CLIPS",
        "required": false,
        "secret": false,
        "desc": "comma-list of CLIP validators that judge image probes, same syntax as PROBE_GENERATORS. Empty or 'none' means the image track does not run at all"
      },
      {
        "key": "PROBE_CLIP_SERVICE",
        "required": false,
        "secret": false,
        "desc": "service name for clip entries that do not name one (default clip-api)"
      },
      {
        "key": "PROBE_QUESTION_FANOUT",
        "required": false,
        "secret": false,
        "desc": "how many nodes (in different /24s) get the same question per round (default 5)"
      },
      {
        "key": "PROBE_FIRE_INTERVAL_SEC",
        "required": false,
        "secret": false,
        "desc": "seconds between firing rounds (default 60)"
      },
      {
        "key": "PROBE_RESPONSE_DEADLINE_SEC",
        "required": false,
        "secret": false,
        "desc": "base answer deadline; scaled up for larger declared models (default 30)"
      },
      {
        "key": "PROBE_REFRESH_SEC",
        "required": false,
        "secret": false,
        "desc": "seconds between node-directory refreshes (default 3600)"
      },
      {
        "key": "PROBE_DB",
        "required": false,
        "secret": false,
        "desc": "path to the shot/question database, relative to the app folder (default probe.db). applyEnvOverrides reads this; it was consumed by the code but undeclared here, so `isann mesh config` could not set it"
      },
      {
        "key": "PROBE_IMAGE_DEADLINE_SEC",
        "required": false,
        "secret": false,
        "desc": "how long a node gets to draw a picture. unset = derived from the node's own reported avg_job_sec (x2, floor 300s, ceiling 900s), which is almost always the right answer"
      },
      {
        "key": "PROBE_FIRE_AT_SELF",
        "required": false,
        "secret": false,
        "desc": "'true' to also fire at this node. off by default - a prober signing tickets for its own node is the shape a faucet must not have; useful for a single-node test bench"
      }
    ]
  }
}
